//Tarjetas
import Card from "../Card";

export function Mujeres() {
  return (
    <section className="cards">
      <h1>Mujeres</h1>
      {/* {cardData.map((card) => (
        <Card
          key={card.id}
          imagen={card.imagen}
          title={card.titulo}
          nombre={card.nombre}
          precio={card.precio}
          card={card}
        /> */}
      {/* ))} */}
    </section>
  );
}
