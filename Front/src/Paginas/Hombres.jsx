//Tarjetas
import Card from "../Card";

export function Hombres() {
  return (
    <section className="cards">
      <h1>Hombres</h1>
      {/* {cardData.map((card) => (
        <Card
          key={card.id}
          imagen={card.imagen}
          title={card.titulo}
          nombre={card.nombre}
          precio={card.precio}
          card={card}
        />
      ))} */}
    </section>
  );
}
